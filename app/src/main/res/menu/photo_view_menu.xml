<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="@string/detect_face"
          android:id="@+id/detect_face_action"
          android:icon="@drawable/ic_detect_face"
          app:showAsAction="ifRoom"/>

    <item android:id="@+id/zoom_action"
          android:icon="@drawable/ic_increase_face_24px"
          app:showAsAction="ifRoom"
          android:title="@string/Zoom"
          android:visible="false"/>

    <item android:title="Back"
    android:id="@+id/back_action"
    app:showAsAction="ifRoom"
    android:visible="false"
    android:icon="@drawable/ic_left_arrow"/>

</menu>